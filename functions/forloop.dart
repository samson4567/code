import 'dart:svg';

void main() {
  // multiple of 4 less than 1000
  // for(var x =4;x <=1000; x+=4){
  //   print(x);
  // }
  // var i = 4;
  // while (i <= 1000) {
  //   print(i);
  //   i+=4;
  //}

  // List names = ["john", "kunle", "bola", "samson"];

  // for (var name in names) {
  //   print('${name} has ${name.length} number of characters');
  // }
  //find the maximum of the number in the array
  List<int> myList = [1, 2, 3, 1, 5, 3, 4, 2, 4, 4, 5, 1];
  print(myList);
  //total number of items 4
  var counter = 0;
  for (var num in myList) {
    if (num == 4) {
      counter++;
    }
  }
  print(counter);

  // miltiples of 2
  var mutiples = 0;
  for (var num in ) {
    if (num == 2) ;
  }
}
